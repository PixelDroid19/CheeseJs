/** Debounce delay in ms before auto-running code after editor changes. */
export const CODE_RUNNER_DEBOUNCE_MS = 150;

/** Timeout in ms for loading the ML language-detection model. */
export const ML_MODEL_LOAD_TIMEOUT_MS = 5_000;

/** Default debounce in ms for language-detection services. */
export const LANGUAGE_DETECTION_DEBOUNCE_MS = 150;

/** Default idle callback timeout in ms for language-detection services. */
export const LANGUAGE_DETECTION_IDLE_TIMEOUT_MS = 100;
